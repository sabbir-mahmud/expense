import Register from "@/components/Ui/Register/Register";

const page = () => {
    return <Register />;
};

export default page;

export const metadata = {
    title: "Register - Expense Manager",
};
